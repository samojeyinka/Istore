<%= form_with(model: [:admin,admin_product]) do |form| %>
  <% if admin_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_product.errors.count, "error") %> prohibited this admin_product from being saved:</h2>

      <ul>
        <% admin_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="crud-form">
    <div class="name-and-img-flex">
      <%= form.text_field :name, placeholder:"Product name" %>
      <div>
        
        <div class="image-upload">
          <label for="file-input">
            <i class="fa fa-camera" title="Upload multiple images" aria-hidden="true"></i>
          </label>
      
          <%= form.file_field :productImages, multiple: true, id:"file-input" %>
      </div> 

      </div>
    </div>
  
  

  <div>
    <%= form.text_area :description, placeholder:"Product description..." %>
  </div>

 <div class="price-active">
  <div>
    <%= form.label :price, style: "display: block" %>₦
    <%= form.number_field :price %>
  </div>

  <div class="active-box">
    <%= form.label :active, style: "display: block" %>:
    <%= form.check_box :active %>
  </div>
 </div>

 <div class="c-input">
  <%= form.label :category_id, style: "display: block" %>
  <%= form.collection_select :category_id, Category.order(:name), :id,:name %>
</div>

<% unless admin_product.new_record? %>
<div class="my-5">
  <%= link_to "Product Stock", admin_product_stocks_path(admin_product), class: "underline" %>
</div>
<% end %>

<div class="c-btn">
  <%= form.submit %>
</div>
</div>
   
 

<% end %>

